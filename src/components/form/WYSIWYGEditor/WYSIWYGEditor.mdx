import {ArgTypes, Canvas, Meta} from '@storybook/addon-docs/blocks';

import * as WYSIWYGEditorStories from './WYSIWYGEditor.stories';

<Meta of={WYSIWYGEditorStories} />

# WYSIWYG Editor

The `WYSIWYGEditor` uses the `proseMirror` library to provide a WYSIWYG editor.

<Canvas of={WYSIWYGEditorStories.Default} />

## Props

<ArgTypes />
